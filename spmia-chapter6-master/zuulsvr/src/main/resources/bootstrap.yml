spring:
  application:
    name: zuulservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
management:
  endpoints:
    routes:
      enabled: true
    filters:
      enabled: true
    web:
      exposure:
        include: "*"
